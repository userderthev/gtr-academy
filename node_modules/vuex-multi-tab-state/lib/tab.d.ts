export default class Tab {
    private tabId;
    private window;
    constructor(window: Window);
    storageAvailable(): Boolean;
    saveState(key: string, state: object): void;
    fetchState(key: string, cb: Function): void;
    addEventListener(key: string, cb: Function): void;
}
